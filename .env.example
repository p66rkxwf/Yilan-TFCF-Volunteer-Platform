# 環境變數範本（複製成 .env.local 後填入真值；請勿提交真實金鑰）
# 本機開發用 .env.local；Cloudflare 部署以 `wrangler secret put` 或 Dashboard 設定。

# --- Supabase（必填）---
# 專案 URL 與 anon key 會進前端 bundle（受 RLS 保護，屬公開資訊）。
NEXT_PUBLIC_SUPABASE_URL=https://<your-project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<supabase-anon-key>

# service role key 具完整資料庫權限，**僅限伺服器端**，切勿加 NEXT_PUBLIC_ 前綴、切勿外流。
SUPABASE_SERVICE_ROLE_KEY=<supabase-service-role-key>

# --- 站台網址（建議設定）---
# 用於組出重設密碼信件的 redirect 連結，固定此值可避免 Host header injection。
# 例：正式站 https://volunteer.example.org ；本機 http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Cloudflare Turnstile（選用：/support 表單防濫用）---
# 兩者皆設定才會啟用；留空則自動停用（表單照常運作）。
# 於 Cloudflare Dashboard → Turnstile 申請。
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# --- 通知寄信 worker（Supabase Edge Function 端設定，非本檔）---
# 寄信金鑰請設為 Supabase Function secret（`supabase secrets set RESEND_API_KEY=...`），
# 不放在 Next 應用的環境變數。詳見 supabase/functions/send-notifications。
