# 環境變數範本（複製成 .env.local 後填入真值；請勿提交真實金鑰）
#
# 本檔僅為「Next.js app 本體」用的變數。分兩類：
#   - NEXT_PUBLIC_*：build-time 變數，會被 inline 進前端 bundle。Cloudflare 部署時
#     必須在 `opennextjs-cloudflare build` 當下就存在（CI 環境變數或 build 前注入）。
#   - 其餘（server-only 機密）：runtime 變數。Cloudflare 以 `wrangler secret put` 或
#     Dashboard 設定，經 OpenNext 於 `process.env` 取用；本機開發放 `.dev.vars`。
#
# 寄信 / 排程 worker 的變數不在本檔——見 workers/orchestrator/.dev.vars.example。

# --- Supabase（必填）---
# 專案 URL 與 anon key 會進前端 bundle（受 RLS 保護，屬公開資訊）。build-time。
NEXT_PUBLIC_SUPABASE_URL=https://<your-project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<supabase-anon-key>

# service role key 具完整資料庫權限，**僅限伺服器端**，切勿加 NEXT_PUBLIC_ 前綴、切勿外流。
# runtime 機密（Cloudflare：`wrangler secret put SUPABASE_SERVICE_ROLE_KEY`）。
SUPABASE_SERVICE_ROLE_KEY=<supabase-service-role-key>

# --- 站台網址（建議設定）---
# 用於組出重設密碼信件的 redirect 連結，固定此值可避免 Host header injection。build-time。
# 例：正式站 https://volunteer.example.org ；本機 http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Cloudflare Turnstile（選用：表單防濫用）---
# 兩者皆設定才會啟用；留空則自動停用（表單照常運作）。
# 於 Cloudflare Dashboard → Turnstile 申請。
# 目前保護 /support；擴充後亦保護 /register、/forgot-password。
NEXT_PUBLIC_TURNSTILE_SITE_KEY=          # build-time（進前端 bundle）
TURNSTILE_SECRET_KEY=                    # runtime 機密（`wrangler secret put`）

# --- 通知寄信 / 背景排程（不在本檔）---
# 由 Cloudflare Cron Worker 負責（workers/orchestrator/）；其機密（RESEND_API_KEY、
# MAIL_FROM、SUPABASE_SERVICE_ROLE_KEY 等）以該 worker 的 `wrangler secret put` 設定，
# 見 workers/orchestrator/README.md 與 .dev.vars.example。
# 另：忘記密碼／註冊驗證信走 Supabase Auth custom SMTP（於 Supabase Dashboard 指向 Resend）。
